[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
upload_speed = 115200
; Sem dependencias externas com o hardware atual (so LDR + LED comum).
; Descomente quando a fita RGB (WS2812B) chegar e o bloco "#if 0 // RGB" for
; reativado em src/main.cpp.
; lib_deps = fastled/FastLED@^3.9.13
